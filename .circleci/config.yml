version: 2.1

orbs: 
  docker: circleci/docker@2.1.2

jobs:
  test-microservice:
    docker:
      - image: python:3.7-alpine3.11
    steps:
      - checkout
      - restore_cache:
          keys: [test-microservice]
      - run: 
          name: Installing Dependencies
          command: |
            apk add build-base
            pip install flask
            pip install pylint
      - run:
          name: Linting Flask Microservice
          command: |
            pylint microservice.py
      - save_cache:
          paths: [cache/test-microservice]
          key: test-microservice

  build-image:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - restore_cache:
          keys: [build-image]
      - run:
          name: Building Docker Image
          command: |
            sudo systemctl start docker
            sudo docker version
            sudo docker image build -t microservice-img .
            sudo dockerpath=rexcode11/udacity-5
            sudo docker login --username rexcode11
            sudo docker tag microservice-img $dockerpath
            sudo docker push $dockerpath
      - save_cache:
          paths: [cache/build-image]
          key: build-image

workflows:
  default:
    jobs:
      - test-microservice:
          filters:
            branches:
              only: [main]
      - build-image:
          requires: [test-microservice]


     